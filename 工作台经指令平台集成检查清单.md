# 工作台经东信指令平台集成执行引擎检查清单

> 适用范围：工作台集成 `workflow-engine-sdk-java`，所有南向 A2A 请求均经东信 Order SDK/指令平台转发到 OMC。  
> 检查基线：2026-09-11 的 `dev` 分支实现。本文是联调准入清单，不替代 API 参考和东信 SDK 原厂文档。

## 1. 先确认边界

“所有操作走指令平台”仅指工作台到被调度 OMC 的南向 A2A 操作。下列调用不经东信 Order SDK：

- WAIMO 经 MSB 调用工作台的北向 A2A 服务端接口。
- 工作台从注册中心获取 AgentCard。
- 工作台从编排中心检索并加载 PSOP/工作流。
- 工作台业务回调对 A2A-T SDK 生成、校验、填充或 LLM 能力的调用。

应经东信 Order SDK 的操作包括：

| 操作 | A2A 路径 | 当前适配器入口 |
|---|---|---|
| Task-T 首次下发 | `POST .../message:send` 或 `POST .../message:stream` | `OrderGatewayClientRuntime.sendMessage` |
| Negotiation-T 后续补参 | 同一 `contextId` 且携带返回的 `taskId` | `OrderGatewayClientRuntime.sendMessage` |
| Authorization-T | 独立的 message send/stream | 独立 runtime/transport |
| Notification-T | 独立的 `message:stream` 长连接 | 独立 runtime/transport |
| 查询任务 | `GET .../tasks/{id}` | `getTask` |
| 列出任务 | `GET .../tasks?...` | `listTasks` |
| 取消任务 | `POST .../tasks/{id}:cancel` | `cancelTask` |
| 订阅已有任务 | `POST .../tasks/{id}:subscribe` | `subscribeToTask` |
| 获取 OMC Token | OMC 登录 HTTP 接口 | 工作台 `AuthProvider` 内经东信 `HttpClient` 调用 |

## 2. 版本与交付物

- [ ] Maven Central 使用 `net.openan.workflow.sdk:workflow-engine:0.0.6`。
- [ ] 工作台需要对外提供 A2A 服务时，同版本引入 `spring-boot-a2a-starter:0.0.6`。
- [ ] A2A Java SDK 基线为 `1.2.0.Final`。
- [ ] A2A-T SDK 基线为 `1.1.0`；业务生成方式引入 `a2a-t-client`，入站校验方式引入 `a2a-t-server`。
- [ ] 东信依赖为 `com.eastcom.apollo:order-shaded-client:1.1.18`，从供应商或企业制品库取得，不等待 Maven Central。
- [ ] 不混用旧 A2A-T API，不使用 `startNegotiation`/`receiveNegotiation`/`continueNegotiation`。
- [ ] `OrderGatewayClientRuntime`、`OrderHttpClientAdapter` 和 `GatewayA2AResponseParser` 当前位于 `samples`，不在 `workflow-engine` Maven jar 内。工作台必须明确是引入内部 Order 适配制品，还是按样例源码维护自己的适配模块。
- [ ] 当前未发布源码已将 Notification-T 首个 ACK 默认值改为 300 秒，并增加 Spring 配置。`0.0.6` 使用方不应假定这一默认值已随制品发布；应显式设置 Builder 或等待包含该修改的新版本。

## 3. 北向 A2A 服务和 MSB

- [ ] 工作台需要接收 WAIMO 任务，必须显式配置 `a2at.server.enabled=true`；starter 默认是 `false`。
- [ ] 配置工作台自身 AgentCard：`a2at.server.agent-card=<location>`。
- [ ] 配置实际服务基路径：`a2at.server.path-prefix=/a2a/json`。
- [ ] MSB 不允许冒号路径时，配置 `a2at.server.slash-action-aliases-enabled=true`，对外可使用 `/message/send`、`/message/stream`、`/tasks/{id}/cancel`、`/tasks/{id}/subscribe`。
- [ ] 保留标准冒号路径与斜杠别名的同一处理逻辑，不复制第二套业务 Controller。
- [ ] MSB 托管的公开前缀如果会在转发前被剔除，不得再写入 `path-prefix`。
- [ ] 已提供 Spring `AgentExecutor` Bean，且入站 Task-T 参数校验失败时返回任务创建前的标准 A2A Error。

## 4. AgentCard、注册中心和编排中心

- [ ] 工作台自身 AgentCard 与被调度 OMC AgentCard 分开管理。
- [ ] 生产从注册中心获取 OMC AgentCard；本地 JSON 只是 Demo 加载方式。
- [ ] 被调度 AgentCard 包含 `HTTP+JSON` interface；Order 适配器不使用 JSON-RPC/gRPC interface 转发。
- [ ] `AgentCard.name` 与 Agent→NE 路由表的 key 完全一致，未命中且没有 `defaultNe` 时应在启动/首次调用时明确失败。
- [ ] AgentCard interface URL 是绝对 URL；Order 模式使用其 scheme 判断 OMC 是否 HTTPS，使用 path/tenant 组装 A2A 路径，实际 OMC 地址由指令平台的 NE 配置解析。
- [ ] `capabilities.streaming` 与 OMC 实际能力一致；Notification-T 要求流式能力。
- [ ] AgentCard 声明 Task-T、Negotiation-T、Authorization-T、Notification-T 的实际支持范围，不因 Demo 而虚报。
- [ ] 编排中心返回的 PSOP 已验证 DAG 无环、节点名称唯一、边目标有效，节点输入选择规则符合业务。
- [ ] 对接真实 OMC 时关闭 Demo 内嵌服务：`a2a.embedded-omc-enabled=false`，不在本地绑定远端 AgentCard 端口。

## 5. Order 平台连接

- [ ] `a2a.transport-mode=order`，生产禁止 `direct` 或 `mock` 被环境变量意外覆盖。
- [ ] `a2a.order.simulator-enabled=false`，生产不启动 `EastcomOrderSimulatorServer`。
- [ ] `EASTCOM_ORDER_HOST/PORT` 是东信指令平台地址，不是 OMC 地址。
- [ ] `EASTCOM_ORDER_USERNAME/PASSWORD/CLIENT_ID/CLIENT_SECRET` 是工作台登录指令平台的凭据，不是 OMC 账号。
- [ ] 每个被调度 Agent 都有确定的 NE 名称，NE 已在指令平台配置真实 OMC 地址和登录凭据替换规则。
- [ ] 已确认平台原样保留 method、path/query、`A2A-Version`、`A2A-Extensions`、`Content-Type`、`Accept`、Authorization、HTTP 状态、多值响应头和 SSE 分片。
- [ ] 当前生产适配使用 `HttpClient.create(serverInfo, config)`，未额外调用静态 `HttpClient.login(...)`。工作台和东信已就 1.1.18 的平台认证契约确认该用法。
- [ ] 不对 Order SDK 的 RSocket 平台连接调用 `.secure(...)`；AgentCard 中的 HTTPS 是平台到 OMC 的目标属性。

## 6. 两层认证不得混用

| 认证层 | 凭据所有者 | 当前入口 |
|---|---|---|
| 工作台→东信指令平台 | Order `ServerInfo` | `EASTCOM_ORDER_*` 平台连接参数 |
| OMC 报文级 Bearer Token | 工作台 `AuthProvider` | `Authorization: Bearer <token>` |

- [ ] 推荐工作台提供名为 `workflowOmcAuthProvider` 的 `AuthProvider` Bean，自行管理 OMC Token 的获取、缓存、刷新和失效。
- [ ] 自定义 Provider 内的 OMC 登录请求也经东信 `HttpClient` 和对应 NE 转发，不从工作台直连 OMC。
- [ ] 登录成功后从 JSON Body 的 `accessSession` 字段取 Token；如现场响应有嵌套，使用实际点路径，例如 `data.accessSession`。
- [ ] `applyAuth(agentName, agentCard, headers)` 在每次请求前注入 Header，Token 必须按 Agent/NE 隔离，Provider 必须线程安全。
- [ ] Order 模式的 `WorkflowEngineClientConfig.credentialsConfigPath` 为 `null`，OMC `Authorization` 只有 `AuthProvider` 一个所有者。
- [ ] 同一个 Provider 可以被 Task-T、Authorization-T、Notification-T 的独立 transport 引用，但 Provider 不得持有“当前通道”等可变全局状态。
- [ ] 不在日志、异常、AgentCard、PSOP 或 Git 中输出平台密码、OMC 密码或完整 Token。

如果暂时使用 Demo 的内置 `EastcomTokenService`，还需检查：

- [ ] `EASTCOM_ORDER_OMC_AUTH_ENABLED=true`。
- [ ] `EASTCOM_ORDER_OMC_CREDENTIALS_PATH` 指向仓库外部的凭据文件。
- [ ] Order Profile 中 `login_url` 仅使用 path/query 转发，实际目标由 NE 决定。
- [ ] `request_fields` 与真实 OMC 登录请求完全一致；需由平台替换的字段才使用 `${ne:username}`/`${ne:password}`。
- [ ] `token_field=accessSession`；`order_token_header` 只是优先检查的可选 Header，不得把真实 Body 字段误当成必有响应头。

## 7. 业务回调和 A2A-T 内容责任

- [ ] `onTask` 只返回最终待发送的 `MessageContent(parts, metadata, extensions)`；引擎不替业务生成或改写 A2A-T 内容。
- [ ] 工作台在 `onTask` 内使用 A2A-T client 的当前生成/填充 API，并确保 extension URI、`templateUri`、metadata 和业务参数匹配。
- [ ] `onNegotiation` 在 OMC 返回 `INPUT_REQUIRED`（或 A2A-T 先协商后建任务模式下的无任务裸 message Propose）且携带有效 Negotiation-T `PROPOSE` 时触发；两种载体的无效协商元数据都显式失败。
- [ ] `onNegotiation` 先校验入站 Propose，再返回 `Send(Accept/Reject/Abort MessageContent)` 或 `Stop`；不自动同意、不重置 `contextId/taskId/round`。
- [ ] `onSelfTask` 执行工作台本地汇总等业务，返回 `TaskResult`；支持多个上游输出，不假定只有单个 LLM 字符串。
- [ ] `onRoute` 在 `0.0.5+` 对每条有条件的出边独立返回 `allow/deny`；无条件边不调用 `onRoute` 且默认放行。不得继续按旧的“N 条边选 1 条”契约实现。
- [ ] 工作台业务回调的 LLM/外部服务超时不超过引擎 callback deadline，且取消后不再发送迟到报文。

## 8. 工作流与独立协议生命周期

- [ ] 两个地市诊断节点是同时 ready 的并行节点，各自使用正确的 Agent、NE 和诊断输入。
- [ ] `ALL_SUCCESS` 汇总节点只在所有活跃前置节点成功后执行一次；某个诊断失败时保留其他已执行结果，但不伪造整体成功。
- [ ] Task-T/Negotiation-T 工作流、Authorization-T 一次性操作、Notification-T 长连接订阅使用不同 runtime/transport/context。
- [ ] runtime supplier 每次返回新的 `OrderGatewayClientRuntime`，不把同一个可变 Order session 实例同时给授权和通知通道。
- [ ] Authorization-T 或 Notification-T 预置失败只记录各自结果，不拦截后续工作流。
- [ ] Notification-T 成功 ACK 后继续保持 SSE，收到抢通终态、工作台显式取消或应用关闭时幂等关闭。
- [ ] 当前不自动重建 Notification-T 订阅；现网需要重连时，工作台必须明确重连、去重和恢复策略。

## 9. 超时、容量和清理

- [ ] `EASTCOM_ORDER_LOGIN_TIMEOUT_SECONDS` 覆盖 OMC Token 登录请求，默认 15 秒是否符合现场时延已验证。
- [ ] `EASTCOM_ORDER_TIMEOUT_SECONDS` 覆盖南向 A2A/Order 请求，Demo 默认 600 秒。
- [ ] Notification-T 首次 ACK 等待目标值为 300 秒；该值只控制首个 ACK，不是 ACK 后 SSE 的空闲时间。
- [ ] 与东信确认 `responseTimeout` 对长时间无心跳 SSE 的真实语义，完成超过 10 分钟的空闲/心跳实测。
- [ ] 工作台启动前只取消本实例确认拥有的历史活跃任务，不在共享账号下无差别取消其他业务任务。
- [ ] Demo `StaleTaskCleaner` 会列出当前身份可见的所有活跃任务并取消，不可未加业务归属过滤就直接复制到生产。
- [ ] 如果现场尚未证明 Order 转发支持 `GET /tasks`、分页和 `POST :cancel`，设置 `task-cleanup-fail-fast=false` 或关闭 Demo 清理，不让清理功能误阻断工作台启动。

## 10. 标准 A2A 错误和业务失败

- [ ] OMC 在任务创建前拒绝请求时，指令平台原样保留非 2xx 状态和 `application/a2a+json` 的顶层 `error` 信封。
- [ ] Order 适配器使用引擎公共 `RemoteA2AErrorException.fromResponse(...)` 解析，不在业务回调里按 400/429 写分支。
- [ ] 流式非 2xx 可能被拆成多个 SDK 回调；适配器先聚合完整 Body，再解析标准错误。
- [ ] `reason=INVALID_PARAMS` 投影为 `a2a.invalid_params`，`ACTIVE_TASK_LIMIT_EXCEEDED` 投影为 `a2a.active_task_limit_exceeded`，无 reason 时回退为 `a2a.http.<status>`。
- [ ] `Retry-After` 仅作为 `errorDetails.retryAfter` 保留，引擎默认不自动重试，避免重复创建业务任务。
- [ ] OMC 任务创建成功后的执行失败返回 HTTP 200 + `TASK_STATE_FAILED`，失败原因放在 TaskStatus message/metadata/artifact，不再返回任务创建前错误信封。
- [ ] 一个并行诊断返回 400/429 时，另一个已下发任务允许完成，汇总节点跳过，工作台北向已创建任务以 `TASK_STATE_FAILED` 结束。

## 11. 路径、流式和协议日志

- [ ] 北向 MSB 可使用斜杠别名；南向 Order 适配器当前按 A2A 标准生成 `message:send`、`message:stream`、`:cancel`、`:subscribe` 路径。两者不是同一个配置点。
- [ ] 如果南向 OMC/MSB 也禁止冒号，需在网关完成重写或增强 Order 适配器；仅修改 AgentCard 基础 URL 不会改变动作后缀。
- [ ] `PROTOCOL` logger 设为 DEBUG，联调时能看到 `ORDER_FORWARD_REQUEST`、`ORDER_SDK_RESPONSE`、SSE 帧、A2A 扩展头和 metadata。
- [ ] `ORDER_FORWARD_REQUEST` 只能证明交给东信 SDK 的内容，`ORDER_SDK_RESPONSE` 只能证明 SDK 回调可见的内容；两者都不是平台→OMC 抓包。
- [ ] `WORKFLOW_ENGINE_PROTOCOL_PRETTY=true`（默认），展示层对 JSON 缩进，SSE JSON 不在每行重复 `data:`。
- [ ] `WORKFLOW_ENGINE_PROTOCOL_INCLUDE_BODY` 和 `WORKFLOW_ENGINE_PROTOCOL_MAX_BODY_CHARS` 根据联调/生产日志策略设置；认证 Header 和口令字段强制脱敏。
- [ ] 每次请求能用 `requestId + executionId + contextId + taskId + agent + NE + channel` 定位，且这些本地关联字段没有污染 A2A metadata。

## 12. 必须现场通过的验收用例

| # | 用例 | 通过标准 | 证据 |
|---|---|---|---|
| 1 | 指令平台登录 | `HttpClient.create` 成功，Agent→NE 正确 | 脱敏日志/平台流水 |
| 2 | OMC Token | 登录请求经 Order 转发，从 Body `accessSession` 取值，业务请求携带 Bearer | 脱敏 Header + OMC 审计 |
| 3 | 北向 MSB | `/message/send` 和 `/message/stream` 可达工作台，冒号标准路径仍可用 | MSB/工作台日志 |
| 4 | 工作流加载 | 注册中心 AgentCard 与编排中心 PSOP 均为真实数据 | 加载日志 |
| 5 | 正常并行诊断 | 两地市任务确实重叠执行，均成功后汇总一次 | 时间戳 + 任务历史 |
| 6 | 单地市协商 | 一个 OMC 缺参返回 Propose，另一个直接诊断；Accept 后只执行一次业务 | Negotiation-T 协议日志 |
| 7 | Authorization-T 失败 | 记录授权失败，后续工作流照常开始 | 独立 context + 工作流日志 |
| 8 | Notification-T | 300 秒内完成 ACK，长连接在抢通结果前保持，结果后关闭 | ACK/事件/关闭日志 |
| 9 | 标准 400 | 投影稳定 `a2a.invalid_params`，不进入协商，汇总不执行 | `errorDetails` + 协议日志 |
| 10 | 标准 429 | 投影 `a2a.active_task_limit_exceeded`，保留 `Retry-After`，不自动重试 | 错误信封 + history |
| 11 | 任务创建后失败 | HTTP 200 + `TASK_STATE_FAILED`，失败证据在状态消息/metadata/artifact | Task-T 终态报文 |
| 12 | list/cancel | 查询参数和分页不丢失，取消后到 `CANCELED` | 平台与 OMC 双侧日志 |
| 13 | 无心跳/半开 | 行为与确认的超时策略一致，不泄漏线程、连接或直接内存 | 长稳记录 |
| 14 | 关闭 | 任务、授权、通知通道分别释放，无重复关闭异常 | shutdown 日志 |

## 13. Go/No-Go 结论

下列任一项未满足时，不应宣布真实环境已端到端通过：

- [ ] 工作台实际使用的 engine/Order/A2A/A2A-T 版本和文档基线一致。
- [ ] 工作台已明确持有 Order 适配器的生产代码，而不是只引入不包含该适配器的 `workflow-engine` jar。
- [ ] 东信已确认 `HttpClient.create` 的认证契约、SSE 超时/心跳、中断和底层连接回收语义。
- [ ] 指令平台已现场证明 message、Negotiation-T follow-up、Authorization-T、Notification-T、list/get/cancel/subscribe 均能原样转发。
- [ ] 两层认证只有各自唯一的 Header/凭据所有者，真实 OMC Token 已从 Body `accessSession` 获取。
- [ ] 直连与 Order 模拟 E2E 是回归证据；上表 14 项真实平台/OMC 用例才是现网准入证据。

## 14. 相关代码与详细文档

- `samples/.../gateway/ClientRuntimeFactory.java`：Spring Order runtime 与 AuthProvider 选择。
- `samples/.../gateway/OrderGatewayClientRuntime.java`：南向 A2A 操作、Agent→NE 路由、会话与错误传播。
- `samples/.../gateway/OrderHttpClientAdapter.java`：东信 `HttpClient` 阻塞/SSE 调用。
- `samples/.../gateway/GatewayA2AResponseParser.java`：A2A 响应与增量 SSE 解析。
- `workflow-engine/.../RemoteA2AErrorException.java`：直连和 Order 共用的标准 A2A 错误模型。
- [指令平台适配指南](docs/zh/指令平台适配指南.md)：Order SDK 调用、配置、超时、日志和已知风险。
- [集成方场景接入指南](docs/zh/scenario/集成方场景接入指南.md)：工作台场景的完整业务接入说明。
- [业务流](docs/zh/scenario/业务流.md)：Task-T、Negotiation-T、Authorization-T、Notification-T 业务时序。
