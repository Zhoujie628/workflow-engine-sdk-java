Extension name: [extension_name]
Input content: [input]
Template URI: [template_uri]
Template content: [template_content]
Parameter schema: [schema]

Apply the system rules and output only the JSON object containing semantic_verdict, errors, and params.
