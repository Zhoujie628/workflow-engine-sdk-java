# 指令平台 SSE 流结束时机问题分析与改进建议

- 文档日期：2026-09-14
- 面向：指令平台及 order-shaded-client SDK 维护方
- 摘要：工作台在一次任务交互中观察到——A2A 终态事件收到之后，流结束信号延迟了约 60 秒才到达。由于工作台侧无法观测平台内部转发过程，本文给出基于客户端日志的现象还原与初步分析（存在两种可能成因），请平台侧协助确认；同时给出无论哪种成因都适用的改进建议。工作台侧已通过客户端自保措施消除了这 60 秒等待，因此本建议为根因修复与健壮性增强，非紧急故障报告。

---

## 1. 现象与分析

### 1.1 链路拓扑

三方角色相互独立：

```
┌─ 工作台 ────────────────────────────┐       ┌─ 指令平台（网侧部署）─────────────────┐       ┌─ OMC ─────┐
│ 工作台侧 SDK (order-shaded-client)   │RSocket│ 转发服务：收内嵌 HTTP 请求 →          │ HTTP  │ A2 agent │
│  HttpClient / sendSse / SseListener  │◄─────►│ 连 OMC → 双向转发（内部实现待确认）    │◄─────►│ (SSE 服务 │
│ （客户端，被动等响应流完成）           │       │                                      │       │  端)      │
└──────────────────────────────────────┘       └──────────────────────────────────────┘       └───────────┘
```

### 1.2 工作台侧观察到的现象（客户端日志，环境信息已脱敏）

一次单地市故障定位任务，流式交互总耗时 375.6 秒，其中有一段**固定的 60 秒零数据等待**：

```
T+0.0s   SSE frame 23 到达：TASK_STATE_COMPLETED, final=true   ← A2A 终态事件已完整送达工作台
T+0.001s 工作台解析层标记终态
T+0.0~60s 无任何数据（终态后心跳亦停止）
T+60.0s  流结束，sendSse 返回                                    ← 恰好在空闲回收周期之后
```

期间工作台侧无任何可继续的操作——`sendSse` 同步阻塞等流完成，`SseListener` 接口无取消手段，只能等待。

### 1.3 初步分析

从客户端日志能确认的事实链：

1. 终态事件在 T+0 已完整送达，工作台侧的解析、业务处理在 T+0 即已就绪；
2. 终态后心跳立即停止。心跳来自 OMC 侧 A2A 服务端，停止时点与终态帧精确吻合，符合 A2A Java SDK 服务端的标准行为（executor 到终态后完成 HTTP 响应）——**据此推断 OMC 侧大概率在终态后正常结束了它的 HTTP 响应**，但这一步工作台侧观测不到（"OMC wire=unobserved"）；
3. 流在整整 60 秒后结束，与空闲回收周期吻合。

由此存在两种可能成因，**从客户端侧无法区分，需平台侧日志确认**：

| 成因 | 过程 | 性质 |
|---|---|---|
| **可能 a**：OMC 已正常结束其响应，但平台转发桥未将"上游响应结束"转为 RSocket 响应流的完成信号，交换挂至空闲回收 | 上游 EOF 发生 → 未及时下发完成 → 60s 后回收兜底 | 转发逻辑缺口（若确认，建议按 2.1 修复） |
| **可能 b**：OMC 未结束其响应（流保持打开），平台如实桥接一条空转流，直至空闲回收 | 上游无 EOF → 无完成信号可发 → 60s 后回收 | OMC 侧行为问题（可与 OMC 侧确认） |

**无论哪种成因，对工作台的表现是相同的：每次任务流到达终态（含协商 INPUT_REQUIRED 轮次）固定多付约 60 秒。** 而改进建议 2.1 对两种成因都有效：a 情况下是修复，b 情况下配合 OMC 侧修正后是正确的传播逻辑。

---

## 2. 改进建议

### 2.1 平台侧：确保"上游 HTTP 响应结束 → RSocket 响应流完成"的传播

建议核对平台转发实现中"读 OMC 响应 → 组装 OrderHttpResponse → 发入响应 Flux"的读循环，确认其上游结束分支是否完成响应流。期望的行为语义如下（示意伪代码，仅描述逻辑，非具体实现）：

```java
// 平台侧：对 OMC 流式响应的转发读循环（行为语义示意）
Flux.create(sink -> {
  while (true) {
    int length = readFromUpstream(buffer);      // 读 OMC 响应
    if (length == IDLE_POLL_TIMEOUT) continue;  // 上游暂无数据（订阅流心跳间隔期），继续等待
    if (length == EOF) break;                   // ★ 上游 HTTP 响应正常结束（chunked 终止块 / 连接正常关闭）
    sink.next(wrapAsOrderHttpResponse(chunk));  // 逐块转发
  }
  sink.next(httpEndChunk());   // ★ 透传 HTTP 终止块，保持转发出的响应自完整
  sink.complete();             // ★ 完成 RSocket 响应流：
                               //   工作台 sendSse 立即返回，该交换的通道即时释放
                               //   （RSocket 多路复用，不影响底层连接与其他交换）
});
```

核心是 EOF 分支后的**透传终止块 + 完成响应流**两步。

上游**异常**断开（如连接被重置、非正常关闭）时也建议同样处理：若已转发过数据，补终止块后完成响应流，让工作台感知结束而不是挂到空闲回收。

**现有的空闲回收机制建议保留**：改动后它退化为兜底安全网，覆盖"上游不结束且工作台也未取消"的孤儿流场景，与新逻辑互不冲突。

### 2.2 SDK 侧（可选增强）：`sendSse` 暴露取消能力

即便平台侧传播逻辑完善，客户端仍需要一个自保手段：SSE 语义上服务端可以无限期不关流（如订阅流），客户端应能在"逻辑上已结束"时主动停止。当前 `sendSse` 返回 void，调用方无从取消。

建议的 API 变更方向（返回可取消句柄）：

```java
// 现状（1.1.19）
public void sendSse(SseListener listener);

// 建议：返回可取消句柄，调用方 cancel(true) 即关闭 SSE 通道、中止底层交换
public Future<Void> sendSse(SseListener listener);
```

A2A 协议的开源 Java SDK（org.a2aproject.sdk，1.2.0）客户端在同场景已有此模式，其 SSE 事件监听基类在收到终态事件后主动取消连接（源码注释原文：*"Client-side auto-close on final events to prevent connection leaks"*）：

```java
protected void handleEvent(StreamingEventKind event, @Nullable Future<Void> future) {
  eventHandler.accept(event);                          // 先把事件交给业务
  if (shouldAutoClose(event) && future != null) {
    future.cancel(true);                               // 收到终态事件 → 主动关闭 SSE 连接
  }
}
```

注：SDK 侧不需要解析 A2A 业务语义（TaskCompleted 等）——只需暴露取消句柄，终态判断留给调用方。不建议在 SDK 内内置"识别 TaskCompleted 自动关流"。

### 2.3 工作台侧现状说明

工作台引擎侧已实现终态主动取消（在 SDK 无取消 API 前提下的等效措施），这 60 秒等待在工作台侧已消除。因此：

- 平台侧确认与改进（2.1）落地后：平台及时发完成信号 + 工作台及时取消，双保险，毫秒级完成；
- 平台侧暂未改动期间：工作台自保已生效，业务不受影响；
- SDK 侧若提供 cancel API（2.2）：工作台可迁移到正式接口（非必须）。

---

## 3. 改动对两类 SSE 流的影响

平台转发的 SSE 流分两类，**建议的改动只会在第一类的情况下触发，对第二类零影响**——因为触发条件是"上游 HTTP 响应结束"这一传输层事件，而非心跳/空闲判定。

### 3.1 主动结束型（任务流，`message:stream`）

```
现状：
OMC: 终态事件 ──► （推断）主动关闭 HTTP 响应
平台: 上游结束后，响应流未立即完成 ──► 60s 空闲回收 ──► 完成响应流
工作台: 终态事件已收到，但 sendSse 阻塞等响应流完成 ──► 等待 60 秒

按建议实施后：
OMC: 终态事件 ──► 主动关闭 HTTP 响应（EOF）
平台: 读到 EOF ──► 立即透传终止块 + 完成响应流（毫秒级）
工作台: sendSse 立即返回，任务流程即时继续
```

### 3.2 长连接型（通知订阅流，Notification-T 类）

```
OMC: 订阅期间 HTTP 响应永不结束（设计如此），持续推送心跳与事件
平台: 上游 EOF 永不发生 ──► "EOF→完成"分支永远不会执行 ──► 行为与现状完全一致
流的生命周期结束方式（与现在相同）：
  - 正常：工作台主动 CANCEL（RSocket 取消帧）──► 平台取消上游连接
  - 兜底：空闲回收（保留），覆盖工作台失联的孤儿流
```

### 3.3 对照表

| 维度 | 主动结束型（任务流） | 长连接型（订阅流） |
|---|---|---|
| OMC 侧行为 | 终态事件后（推断）主动完成 HTTP 响应 | 订阅期内响应永不结束，心跳保活 |
| 上游 EOF | 终态后毫秒级发生 | **永不发生**（agent 崩溃除外） |
| 新逻辑是否触发 | 触发 → 立即完成响应流 | **不触发**（分支对它是死代码） |
| 实施前工作台表现 | 等 60s 空闲回收 | 靠心跳维持，工作台 cancel 关闭 |
| 实施后工作台表现 | 毫秒级返回 | **完全不变** |
| 边界情况 | — | agent 崩溃/异常断开时上游 EOF 发生 → 工作台**更早**感知订阅中断（改善，原需等空闲回收） |

### 3.4 需要避开的改法

| 做法 | 风险 |
|---|---|
| 把空闲回收从 60s 调小（如 5s）"流空闲就关" | 定时器驱动会误杀订阅流：两次事件之间的合法静默期、心跳间隔调整、事件稀疏场景都会被掐断 |
| "检测到心跳停止就关流" | 心跳是实现细节而非协议保证；且心跳停止的原因恰恰可能是"流已被服务端正常关闭"，此时该传播的是 EOF 而不是猜测 |
| SDK 内置解析 TaskCompleted 自动关流 | 传输 SDK 不应理解应用协议；SSE 载荷还可能是错误信封等非 A2A 内容，解析脆弱且层次错误 |

**建议的原则：只由"上游流结束"这一确定性传输事件驱动，不做任何基于时间或心跳的推断。**

---

## 4. 请平台侧协助确认的事项

为区分 1.3 中的两种成因，请协助提供或核对以下信息（对应同一次交换的平台侧日志/时间点）：

1. 该交换中，平台到 OMC 的上游 HTTP 连接**实际关闭的时刻**（若平台侧有连接生命周期日志）；
2. 平台向工作台下发的 RSocket 响应流**完成的时刻**，以及该完成是由什么触发的（上游结束 / 空闲回收 / 其他）；
3. 平台转发实现中，上游响应结束后是否已有完成响应流的逻辑（若有，可能是未覆盖到该场景，欢迎反馈具体行为）；
4. 空闲回收的配置周期与是否可按交换类型区分。

若确认为成因 a：建议按 2.1 补齐传播逻辑；若为成因 b：建议与 OMC 侧确认其 A2A 服务端在终态后是否完成响应（A2A Java SDK 服务端的标准行为是完成后关闭），平台侧按 2.1 保证传播即可。

---

## 5. 实施后的验证建议

| 项 | 建议 |
|---|---|
| 任务流终态时延 | 终态事件转发完成后，响应流在 1 秒内完成（预期毫秒级）；工作台 `sendSse` 返回不再出现固定 ~60s 平台段 |
| 正常完成路径 | 上游正常关闭 → 透传终止块 → 完成响应流，转发出的响应自完整、可被工作台正常解析 |
| 异常断开路径 | 上游异常断开 → 已转发数据时补终止块并完成，工作台可感知结束 |
| 订阅流回归 | 长连接订阅建立后持续存活（建议 ≥ 数分钟），心跳/事件正常转发；工作台主动取消后通道即时释放；空闲回收兜底仍在 |
| 并发隔离 | 同一 RSocket 连接上任务流与订阅流并存时，完成任一任务流的响应流不影响另一条及后续 `loadNeResource` 等调用 |
| 日志可观测 | 流完成路径输出明确日志标记，便于现场定位 |
